<!-- Announcement Bar -->
<section id="announcement-bar" class="announcement">
  <div class="container">
    <p class="lead">{{ settings.announcement_text | default: 'Free shipping on orders over $50' }}</p>
  </div>
</section>
